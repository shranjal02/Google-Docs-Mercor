# Google-Docs-Mercor
Create a frontend clone of Google Docs' single document editor screen using ReactJS. The goal is not to build out the entire functionality of Google Docs, but rather to replicate the interface and interactions of the document editing view.
